spring.application.name=somdelie-pos

# Neon PostgreSQL Database Configuration
spring.datasource.url=${DATA_URL}
spring.datasource.username=${DATASOURCE_USER}
spring.datasource.password=${DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Server Configuration
server.port=${PORT:5000}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.secret=${JWT_SECRET:jdjjfuttw65473940wisojifssnvskncslmsojkdoakjdhauitdqiwplkedhwuet280skfák.hxlvbd ctq52340t7rw54,t}
app.jwt.expiration-seconds=${JWT_EXPIRATION:86400}